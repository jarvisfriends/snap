// Package charts holds terminal chart primitives:
// sparklines (block and braille), horizontal bars, pie charts, sankey flows,
// multi-series braille line charts, and the pixel Canvas they draw on. All
// take explicit dimensions and render lipgloss-styled strings; colors come
// from the shared snap/styles palette.
package charts
